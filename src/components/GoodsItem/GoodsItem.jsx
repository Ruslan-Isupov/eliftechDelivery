export const GoodsItem = ({ name, price, idGood }) => {
    return (
        <li>
            <p>{name}</p>
             <p>{ price}</p>
            <img src="" alt="" />
            <button></button>
        </li>
    )
}